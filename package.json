{
  "name": "ittysockets.com",
  "version": "0.1.1",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "release": "release --push --nopublish --patch"
  },
  "devDependencies": {
    "sass": "^1.85.0",
    "vitepress": "2.0.0-alpha.2",
    "yarn-release": "^1.10.6"
  }
}
